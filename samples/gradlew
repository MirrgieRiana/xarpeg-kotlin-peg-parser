#!/usr/bin/env sh

DIR="$(cd "$(dirname "$0")" && pwd)"
PARENT_GRADLEW="$DIR/../gradlew"

if [ ! -x "$PARENT_GRADLEW" ]; then
  echo "gradlew not found at $PARENT_GRADLEW" >&2
  exit 1
fi

exec "$PARENT_GRADLEW" "$@"
